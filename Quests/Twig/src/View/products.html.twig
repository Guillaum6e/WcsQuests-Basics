<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Bonjour</title>
    <link rel="icon" href="/assets/images/favicon.png">
    <link rel="stylesheet" href="/assets/css/styles.css">
</head>

<body>
    <p> {% for category, product in products %}
        <li>The product {{ product }} in category {{ category }}.</li>
        {% else %}
    <p>Mon tableau est vide !</p>
        {% endfor %}
        {{ dump(products) }}
    </p>
</body>
</html>